module Main exposing (main)

import Test
import Test.Runner.Node as TestRunner
import UnifiedTests
import PerfTest


main : TestRunner.Program
main =
    TestRunner.run <|
        Test.describe "gren-diff tests"
            [ UnifiedTests.tests
            , PerfTest.tests
            ]
